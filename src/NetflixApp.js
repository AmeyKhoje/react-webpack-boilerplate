const NetflixApp = (props) => {
  return (
    <div>
      <h1>
        React, Webpack, Eslint, Prettier, SASS basic boilerplate by {'{Amey}'}
      </h1>
    </div>
  );
};

export default NetflixApp;
